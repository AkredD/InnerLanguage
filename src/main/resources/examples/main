TYPE Main.
	STATIC (
		DATA: Integer a VALUES(412).
		CONSTANT: Integer b VALUES(4321).
	)
	FUNCTION Integer main (Integer e)
		START.
			DATA: Integer g VALUES(a).
			if ((a == 412) AND FALSE)
				START.
					DATA: Boolean gg VALUES(true).
					return a.
				END.
			ELSE
				START.
					DATA: Boolean gg VALUES(true).
				END.
			DATA: Boolean gg VALUES(true).
			data: Integer i values (41).
			while (i > 0)
				START.
					continue.
				END.
			return g.
		END.

	FUNCTION Integer mainA ()
		START.
			if (a == 12 AND FALSE)
				START.

				END.
			ELSE
				START.
					return a.
				END.
			data: Integer i values (41).
			while (((Integer) b) > 0)
				START.
					continue.
				END.
			return a.
		END.
END.